export enum Endpoint {
  Login = "/auth/local",
  Me = "/users/me",
  NewService = "/services",
  ServiceList = "/services",
  GetServiceById = "/services",
  Upload = "/upload",
  Logout = "/logout",
}
