export enum Endpoint {
  Login = "/auth/local",
  Me = "/users/me",
  NewService = "/services",
  UpdateService = "/services",
  ServiceList = "/services",
  GetServiceById = "/services",
  Upload = "/upload",
  Logout = "/logout",
}
